# Quickstart Guide

## Setup
1. Install project dependencies in editable mode:
   ```bash
   pip install -e .[dev]
   ```

2. Run training:
   ```bash
   antigrav train --timesteps 10000
   ```

3. Launch live trading in dry run:
   ```bash
   antigrav live --dry-run
   ```
