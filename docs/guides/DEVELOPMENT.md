# Development Guide

## Code Structure
* `src/antigravity/`: Subsystem modules (features, core, database, overseer, gateway, etc.)
* `src/cli/`: Click commands logic
* `tests/`: Pytest tests

## Running Tests
Run pytest in the virtual env:
```bash
pytest
```
